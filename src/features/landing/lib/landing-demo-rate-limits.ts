/**
 * Landing demo rate policy.
 *
 * Per-IP trial caps are off — they previously blocked real demos.
 * Public Talk/Voice skip IP/platform buckets.
 */

export const LANDING_DEMO_RATE_ENABLED = false;
